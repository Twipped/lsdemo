Lifespark Demo Project
===

To run locally:

```
npm install
npm start
```

This will launch a web server and a watch process. Wait for the script to say a bundle is finished before loading.

---

To build for production:
```
NODE_ENV=production npm run build
```
